name: Build boykisser-clan

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup MSBuild
      uses: microsoft/setup-msbuild@v2
      
    - name: Setup vcpkg and install dependencies
      run: |
        git clone https://github.com/microsoft/vcpkg.git
        .\vcpkg\bootstrap-vcpkg.bat
        .\vcpkg\vcpkg install openssl:x64-windows zstd:x64-windows xxhash:x64-windows
      shell: cmd

    - name: Copy vcpkg headers and libs to project dir
      run: |
        xcopy /E /Y vcpkg_installed\x64-windows\include\openssl boykisser-clan\include\openssl\
        copy /Y vcpkg_installed\x64-windows\include\xxhash.h boykisser-clan\include\xxhash.h
        copy /Y vcpkg_installed\x64-windows\include\zstd.h boykisser-clan\include\zstd.h
        mkdir boykisser-clan\lib 2>nul
        copy /Y vcpkg_installed\x64-windows\lib\libcrypto.lib boykisser-clan\lib\
        copy /Y vcpkg_installed\x64-windows\lib\libssl.lib boykisser-clan\lib\
        copy /Y vcpkg_installed\x64-windows\lib\zstd.lib boykisser-clan\lib\
        copy /Y vcpkg_installed\x64-windows\lib\xxhash.lib boykisser-clan\lib\
        echo "Verifying files copied:"
        dir boykisser-clan\include\openssl\err.h
        dir boykisser-clan\include\xxhash.h
        dir boykisser-clan\lib\
      shell: cmd

    - name: Build C++ project
      run: |
        cd boykisser-clan
        msbuild boykisser-clan.vcxproj /p:Configuration=Release /p:Platform=x64 /verbosity:detailed
      shell: cmd

    - name: Build C# project
      run: |
        msbuild boykisserclan\boykisserclan.csproj /p:Configuration=Release /p:Platform=x64 /verbosity:normal
      shell: cmd
      working-directory: boykisserclan

    - name: Create Release
      uses: softprops/action-gh-release@v2
      with:
        tag_name: v${{ github.run_number }}
        name: Build ${{ github.run_number }}
        files: |
          boykisser-clan/build/Release/boykisser-clan.exe
          boykisserclan/build/Release/net8.0-windows/boykisser-clan.exe
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
